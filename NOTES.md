# NOTES.md

## What was put in the CLAUDE.md file
- Commands to run, test and lint the project
- Conventions covering imports/exports, linting, routing and data access
- architecture guidance covering entry point, database and config

## Permission rules
- Allow running `npm test`
- Ask when running `git push`
- Deny reading from `.env`
- Deny running `git push -f`